services:
  - type: web
    name: hassan-wilson
    runtime: node
    plan: free
    buildCommand: npm run render-build
    startCommand: npm start
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: "true"
